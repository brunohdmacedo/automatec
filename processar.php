<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $nome = $_POST["nome"];
    $email = $_POST["email"];

    // Aqui você pode processar os dados, enviar e-mails, armazenar no banco de dados, etc.
    // Neste exemplo, não estamos fazendo nada com os dados recebidos.
}
?>
